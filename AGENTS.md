# Kalivra 저장소 작업 지침

## 적용 범위

이 파일은 저장소 전체에 적용한다. 더 좁은 디렉터리에 `AGENTS.md` 또는
`AGENTS.override.md`가 생기면 해당 영역의 구체적인 지침을 함께 따른다.

Kalivra는 Electron, CommonJS, HTML/CSS/JavaScript로 만든 게임 밸런스 분석 도구다.
리팩터링의 우선순위는 다음과 같다.

1. 사용자 데이터와 기존 동작의 안정성
2. 명확한 책임과 의존성 방향
3. 테스트 가능성과 관찰 가능한 오류 처리
4. 확장성과 유지보수성
5. 코드 양 감소나 패턴 적용 자체

SOLID는 판단 기준이지 패턴 적용 할당량이 아니다. 새 추상화는 실제 변경 축,
대체 구현, 테스트 대역 또는 프로세스 경계를 분리할 때만 추가한다.

## 반드시 보존할 제품 계약

- 기존 `.kal` 파일을 계속 읽을 수 있어야 한다. 저장 스키마를 바꾸면 명시적인
  스키마 버전, 이전 버전 마이그레이션, 실패 시 사용자에게 보이는 오류를 함께 둔다.
- 저장, 불러오기, 스냅샷, 아이템 세트, Undo/Redo, CSV/JSON 및 Unity/Unreal 내보내기,
  시뮬레이션 결과와 전투 로그를 의도 없이 변경하지 않는다.
- 밸런스 계산식, 레벨 성장, 아이템 modifier/trait, 명중·치명타·회피·버프 규칙은
  도메인 계약이다. 계산 변경은 단순 구조 변경과 분리하고 대표 fixture로 검증한다.
- 취소한 파일 대화상자, 잘못된 파일, 손상된 수식, 저장 실패를 정상 흐름과 구분한다.
  오류를 조용히 `0`, `null`, 성공 문자열로 바꾸지 않는다.
- 사용자 프로젝트 원본을 마이그레이션 또는 정규화 과정에서 직접 훼손하지 않는다.

## 목표 의존성 방향

다음 경계를 지향하되 한 번에 전체 구조를 교체하지 않는다.

`Electron/DOM/Firebase/파일 시스템 어댑터 -> 애플리케이션 유스케이스 -> 순수 도메인`

- 도메인: 스탯 성장, 수식 계약, 전투 규칙, snapshot 가능한 프로젝트 모델.
  Electron, DOM, Chart.js, Firebase, 전역 상태에 의존하지 않아야 한다.
- 애플리케이션: 명령 실행, Undo/Redo, 프로젝트 저장·불러오기, 시뮬레이션 조정,
  export 같은 유스케이스를 조합한다. 구체 어댑터 대신 작은 포트에 의존한다.
- 어댑터: IPC, 파일 대화상자와 파일 시스템, Firebase, Chart.js, DOM 렌더링을 담당한다.
- 조립 지점: `main.js`, preload/renderer bootstrap에서 구체 구현을 연결한다.

새 코드가 이 방향을 거슬러 도메인에서 Electron이나 DOM을 import하지 않게 한다.
기존 코드의 역방향 의존성은 건드리는 기능 주변부터 얇은 seam으로 분리한다.

## 저장소 특화 설계 규칙

- `renderer.js`에 새 기능 로직을 계속 쌓지 않는다. 이벤트 연결과 화면 조정만 남기고,
  도메인 계산·상태 변경·내보내기 생성은 응집된 모듈로 추출한다.
- 프로젝트 상태와 명령 이력의 소유자는 각각 하나여야 한다. 현재 명령 이력은
  `src/application/commandManager.js`의 한 인스턴스로 통합되어 있다. 새 Undo/Redo 스택을
  만들지 말고, 남은 구체 편집 명령을 애플리케이션 경계로 점진적으로 이동한다.
- 외부에 반환한 가변 배열·객체를 임의로 수정하는 새 코드를 만들지 않는다. 변경은
  이름 있는 명령/유스케이스를 통하고, 저장·Undo에 필요한 snapshot 정책을 명시한다.
- 전투 난수와 ID/시간 생성은 주입 가능한 함수로 감싸 결정적 테스트가 가능해야 한다.
- 동적 수식 실행은 하나의 formula-engine 경계에 격리한다. `new Function` 또는 유사한
  실행을 UI, 전투, solver에 새로 흩뿌리지 말고 허용 문법·식별자·유한 숫자 결과를 검증한다.
- IPC 채널은 중앙 계약으로 관리하고 payload와 반환값을 양쪽 경계에서 검증한다.
  renderer에 파일 시스템이나 Electron 전체 권한을 새로 노출하지 않는다.
- Electron 보안 설정을 악화시키지 않는다. 창/IPC를 다루는 리팩터링은 preload와
  `contextBridge`를 통한 최소 API, `contextIsolation: true`, `nodeIntegration: false`로
  이동하는 방향이어야 하며 전환 중 호환 단계를 명시한다.
- 사용자 또는 파일에서 온 문자열을 `innerHTML`에 바로 넣지 않는다. 정적 템플릿과
  동적 텍스트를 분리하고 동적 값에는 `textContent`나 안전한 DOM 생성을 사용한다.
- 파일 I/O는 main 프로세스의 어댑터에 둔다. 새 동기 I/O를 추가하지 말고, 저장은
  가능한 한 임시 파일 작성 후 교체하는 원자적 비동기 흐름을 사용한다.
- Firebase 피드백은 선택적인 외부 어댑터다. 실패가 핵심 편집·시뮬레이션 흐름을
  중단시키지 않게 하고, 피드백 내용이나 사용자 데이터를 로그에 남기지 않는다.

## 작업 방식

- 시작할 때 `git status --short`와 관련 호출자/소비자를 확인한다. 사용자의 기존 변경을
  덮어쓰거나 관련 없는 파일을 정리하지 않는다.
- 큰 재작성 대신 되돌릴 수 있는 수직 단위로 작업한다: 현재 동작 고정 -> seam 추가 ->
  책임 이동 -> 호출부 전환 -> 죽은 코드 제거. 구조 변경과 동작 변경을 같은 단계에 섞지 않는다.
- 추출 전에 대표 정상·경계·실패 동작을 characterization test로 고정한다. 테스트하기
  어려운 코드는 먼저 시간, 난수, 저장소, formula evaluator 같은 비결정 경계를 주입한다.
- public 모듈 계약과 저장 형식을 바꿀 때는 모든 호출자, fixture, 문서를 같은 변경에서 갱신한다.
- 새 production 의존성은 기존 플랫폼이나 작은 로컬 모듈로 해결할 수 없는 이유와
  번들 크기·보안·유지보수 영향을 먼저 설명한다.
- 존재하지 않는 명령이 성공했다고 보고하지 않는다. `npm test`로 자동 테스트를 실행하고,
  변경한 JavaScript는 `node --check` 또는 renderer build로 파싱을 검증한다. 관련 수동
  검증 범위와 미검증 항목도 명시한다.

## 현재 명령

- 의존성 설치: `npm install` 또는 lockfile을 보존하는 CI 설치 시 `npm ci`
- 자동 테스트: `npm test`
- renderer 번들: `npm run build:renderer`
- 개발 실행: `npm start`
- 패키징: `npm run build:win`, `npm run build:linux`, `npm run build:mac`, `npm run build:all`
- JavaScript 파싱 확인: `node --check <changed-file.js>`

패키징은 느리고 플랫폼 영향을 받으므로 배포/설정 변경에 맞춰 실행한다. GUI 검증을
하지 못한 경우 검증했다고 추정하지 말고 그 사실을 결과에 적는다.

## Code Review Rules

다음 항목은 회귀 위험이 높으므로 리뷰에서 구체적으로 지적한다.

- `.kal` 또는 export 스키마 변경에 버전/마이그레이션/호환 fixture가 없음
- 계산·전투 로직 변경에 결정적 테스트가 없거나 `Math.random`, `Date.now`가 직접 결합됨
- 명령이 일부 상태만 되돌리거나 Undo/Redo 스택이 여러 소유자에게 추가됨
- renderer에 Node/Electron/Firebase/파일 시스템 권한 또는 새 IPC 채널이 무검증으로 노출됨
- 외부 입력이 `innerHTML` 또는 동적 코드 실행으로 직접 전달됨
- catch가 데이터 손실·계산 실패를 숨기거나 성공처럼 처리함
- 도메인 모듈이 DOM, Electron, Chart.js, Firebase 같은 구체 인프라에 의존함
- 추출 명목으로 책임이 불명확한 manager/helper/service 또는 순환 의존성을 추가함
